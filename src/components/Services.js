export default function Services({ number }) {
    return (
        <div className="container">
            <h1 className="text_white">This is the services page - {number}</h1>
        </div>
    );
}
